export const goToHome = (navigation) => {
    navigation('/')
}

export const goToPokedex = (navigation) => {
    navigation('/pokedex')
}

export const goToDetails = (navigation) => {
    navigation('/detalhepokemon')
}
